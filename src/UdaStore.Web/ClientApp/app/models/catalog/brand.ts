import { KeyValue } from "./key-value";

export class Brand extends KeyValue {
    
    seoTitle: string;
            
    description: string;
    
    isPublished: boolean;
    
    isDeleted: boolean;
}